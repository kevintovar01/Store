services:
  react-app:
    container_name: react_app
    build:
      context: ./BaseDrop2/project/Dockerfile
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    environment:
      - CHOKIDAR_USEPOLLING=true
